'use server';import { z } from 'zod';import { requireCompanyAccess } from '@/lib/auth';import { getDre } from '../dre/actions';import { getCashFlowProjection } from '../cash-flow/actions';import { getBudgetOverview } from '../budgets/actions';
const Schema=z.object({company_id:z.string().uuid(),year:z.number().int().min(2020).max(2100)});export async function getExecutiveFinancialReport(input:unknown){const data=Schema.parse(input);await requireCompanyAccess(data.company_id,'view');const[dre,cashFlow,budget]=await Promise.all([getDre(data),getCashFlowProjection(data),getBudgetOverview(data)]);return{year:data.year,dre,cashFlow,budget,generatedAt:new Date().toISOString()};}
