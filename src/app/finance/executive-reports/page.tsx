import { getCompanies } from '@/app/actions';import ReportClient from './ReportClient';import { getExecutiveFinancialReport } from './actions';export const dynamic='force-dynamic';export default async function ExecutiveReports(){const companies=await getCompanies();const company=companies[0];if(!company)return <div>Cadastre uma empresa.</div>;const year=new Date().getFullYear();const data=await getExecutiveFinancialReport({company_id:company.id,year});return <ReportClient companies={companies} initialCompany={company.id} initialYear={year} initialData={data}/>}
