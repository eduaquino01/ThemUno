import { getCompanies } from '@/app/actions'; import { getDre } from './actions'; import DreClient from './DreClient'; export const dynamic='force-dynamic'; export default async function DrePage(){const year=new Date().getFullYear();const [companies,data]=await Promise.all([getCompanies(),getDre({company_id:'ALL',year})]);return <DreClient companies={companies} initialData={data} initialYear={year}/>;}
