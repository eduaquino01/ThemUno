import { getCompanies } from '@/app/actions';import ScenarioClient from './ScenarioClient';import { getScenarioOverview } from './actions';export const dynamic='force-dynamic';export default async function ScenariosPage(){const companies=await getCompanies();const company=companies[0];if(!company)return <div className="p-8 text-slate-400">Cadastre uma empresa para criar cenários.</div>;const year=new Date().getFullYear();const data=await getScenarioOverview({company_id:company.id,year});return <ScenarioClient companies={companies} initialCompany={company.id} initialYear={year} initialData={data}/>}
